---
import LandingSubtitle from './LandingSubtitle.astro';
// import { ChevronDownOutline } from 'svelte-ionicons';
import { useTranslations } from '$i18n/utils';
import { indexTranslations } from '$i18n/translations';

const language = Astro.currentLocale;
const t = useTranslations(indexTranslations, language as keyof typeof indexTranslations);

const subtitles = [
    { emoji: '👨‍🎓', text: t('landing.cs-student') },
    { emoji: '⌨️', text: t('landing.fullstack-dev') },
    { emoji: '🇫🇷', text: t('landing.french') },
    { emoji: '💻', text: t('landing.it-passionate') },
    { emoji: '🐈', text: t('landing.love-cats') },
];
const delayPerSubtitle = 4;
---

<section>
    <div class="content">
        <h1>
            {t('landing.hello')}
            <br>
            {t('landing.im')} <span class="text-gradient">Julien</span>
        </h1>
        <div class="subtitle-container">
            {subtitles.map((subtitle, index) => (
                    <LandingSubtitle
                            emoji={subtitle.emoji}
                            text={subtitle.text}
                            delay={index * delayPerSubtitle}
                            animationDuration={delayPerSubtitle * subtitles.length}/>
            ))}
        </div>
    </div>

    <!--<a href="#about" class="arrow-down" aria-label="Scroll down to about section">-->
    <!--    <ChevronDownOutline size="2rem" color="var(&#45;&#45;text-color)"/>-->
    <!--</a>-->
</section>

<style>
    section {
        width: 100%;
        height: 100svh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .content {
        z-index: 2;
    }

    h1 {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: .75rem;
    }

    .text-gradient {
        background: linear-gradient(90deg, var(--gradient-blue), var(--gradient-purple)) 50% 0;
        background-clip: text;
        background-size: 200% 100%;
        -webkit-text-fill-color: transparent;
    }

    .subtitle-container {
        position: relative;
    }

    .arrow-down {
        position: absolute;
        bottom: 0;
        padding: 2rem;
        animation: bounce 2s infinite alternate ease-in-out;
    }

    @keyframes bounce {
        to {
            transform: translateY(.5rem);
        }
    }
</style>
