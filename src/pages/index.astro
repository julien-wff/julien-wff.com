---
import ResumeBox from '../components/resume/ResumeBox.svelte';
import ResumeHeader from '../components/resume/ResumeHeader.svelte';
import Layout from '../layouts/Layout.astro';
---

<Layout title="Julien Wolff - CV">
    <div class="resume">
        <ResumeHeader />
        <div class="content">
            <div>
            </div>
            <div>
            </div>
        </div>
    </div>
</Layout>

<style>
    .resume {
        width: 100%;
        max-width: 50rem;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        gap: var(--spacing);
    }

    .content {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: var(--spacing);
    }
</style>
